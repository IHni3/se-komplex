package employee;

public class Administrator extends Employee {

    public Administrator(int id, String name, Roles role, String pin, String superPin) throws Exception {
        super(id, name, role, pin, superPin);
    }
}
