package employee;

public class DataAnalyst extends Employee {
    public DataAnalyst(int id, String name, Roles role, String pin, String superPin) throws Exception {
        super(id, name, role, pin, superPin);
    }
}
