package employee;

public class Operator extends Employee {
    public Operator(int id, String name, Roles role, String pin, String superPin) throws Exception {
        super(id, name, role, pin, superPin);
    }
}
